spring:
  application:
    name: enes-durmus-case

  datasource:
    url: jdbc:postgresql://localhost:5432/case_db
    username: case_user
    password: case_pass

  data:
    redis:
      host: localhost
      port: 6379
